<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ashok_ray.security">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".osafirebase"
        android:allowBackup="true"
        android:icon="@mipmap/logomak"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login"
            android:noHistory="true" />
        <activity
            android:name=".Module_HOME.ACT_Home"
            android:noHistory="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".register"
            android:noHistory="true" />
        <activity android:name=".setting" />
        <activity android:name=".changeUser" />
        <activity android:name=".reader_interface" />
        <activity android:name=".master_chat_private" />
        <activity android:name=".ssh_setting"></activity>
    </application>

</manifest>